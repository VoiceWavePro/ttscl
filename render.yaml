services:
  - type: web
    name: tts-app
    runtime: docker
    env: python
    buildCommand: docker build -t tts-app .
    startCommand: python app.py
    port: 5000
    # Add health check path (optional but recommended)
    healthCheckPath: /tts
